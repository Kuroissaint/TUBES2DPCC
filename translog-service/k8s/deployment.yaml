apiVersion: apps/v1
kind: Deployment
metadata:
  name: translog-service
  labels:
    app: translog-service
spec:
  replicas: 1 # Set ke 1 dulu untuk hemat resource lokal
  selector:
    matchLabels:
      app: translog-service
  template:
    metadata:
      labels:
        app: translog-service
    spec:
      containers:
      - name: translog-service
        image: my-registry/translog-service:latest # Nanti di-replace oleh Jenkins
        ports:
        - containerPort: 8008
        env:
        - name: DB_HOST
          value: "db-postgres-service"
        - name: DB_NAME
          value: "db_shopping_order"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "password"